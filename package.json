{
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "predeploy": "node scripts/bump-cache.js",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "dotenv": "^17.4.2",
    "firebase": "^12.11.0",
    "firebase-admin": "^14.0.0"
  },
  "devDependencies": {
    "jest": "^30.4.2",
    "jest-environment-jsdom": "^30.4.1"
  }
}
